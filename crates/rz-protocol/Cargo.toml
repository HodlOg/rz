[package]
name = "rz-protocol"
version = "0.3.2"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Wire protocol types for rz agent-to-agent messaging"

[dependencies]
eyre = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
